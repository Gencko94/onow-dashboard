import styled from 'styled-components';

const HeaderCustomizer = () => {
  return <Container>Header Customizer</Container>;
};

export default HeaderCustomizer;
const Container = styled.div`
  padding: 0.75rem;
`;
