import React, { useState } from "react";
import styled from "styled-components";

const Tooltip = () => {
  return <Container>Hello from tooltip</Container>;
};

export default Tooltip;
const Container = styled.div`
  background-color: #fff;
  padding: 1rem;
  border-radius: 6px;
`;
